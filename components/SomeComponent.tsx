// components/SomeComponent.tsx
import { useRouter } from '../utils/routing';

const SomeComponent = () => {
  const router = useRouter();

  // Use router for logic that needs access to routing context
  // ... rest of the component
};
